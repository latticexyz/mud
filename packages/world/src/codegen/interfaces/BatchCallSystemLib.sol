// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { IWorldKernel } from "../../IWorldKernel.sol";
import { WorldContextConsumerLib } from "../../WorldContext.sol";
import { ResourceId, WorldResourceIdLib } from "../../WorldResourceId.sol";
import { RESOURCE_SYSTEM } from "../../worldResourceTypes.sol";

import { SystemCallData, SystemCallFromData } from "./../../modules/init/types.sol";
import { IBatchCallSystem } from "./IBatchCallSystem.sol";

library Utils {
  bytes14 constant namespace = "";
  bytes16 constant name = "BatchCallSystem";

  function systemId(bytes14 _namespace) internal pure returns (ResourceId) {
    return WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: _namespace, name: name });
  }

  function systemId() internal pure returns (ResourceId) {
    return systemId(namespace);
  }
}

/**
 * @title BatchCallSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library BatchCallSystemLib {
  function batchCall(SystemCallData[] calldata systemCalls) internal returns (bytes[] memory returnDatas) {
    return
      abi.decode(
        IWorldKernel(WorldContextConsumerLib._world()).call(
          Utils.systemId(),
          abi.encodeCall(IBatchCallSystem.batchCall, (systemCalls))
        ),
        (bytes[])
      );
  }

  function batchCallFrom(SystemCallFromData[] calldata systemCalls) internal returns (bytes[] memory returnDatas) {
    return
      abi.decode(
        IWorldKernel(WorldContextConsumerLib._world()).call(
          Utils.systemId(),
          abi.encodeCall(IBatchCallSystem.batchCallFrom, (systemCalls))
        ),
        (bytes[])
      );
  }
}
