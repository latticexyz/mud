import { describe, it } from "vitest";

describe("queryFragments", () => {
  describe("In", () => {
    it.todo("should match all records in the table");
  });

  describe("NotIn", () => {
    it.todo("should match all records not in the table");
  });

  describe("MatchRecord", () => {
    it.todo("should match all records matching the provided partial value");
  });

  describe("NotMatchRecord", () => {
    it.todo("should match all records not matching the provided partial value");
  });
});
