import "../src/sqlite/trpcServer";
import "../src/sqlite/indexer";

// For the purposes of this mockup, we're using an in-memory "database", so we need both tRPC and the indexer to be in the same process.
