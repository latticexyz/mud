import { AccountModalContent } from "../../AccountModalContent";

export function StandardBridgeContent() {
  return (
    <AccountModalContent title="Bridge balance top-up">
      <div className="flex flex-col gap-2">Bridge balance top-up</div>
    </AccountModalContent>
  );
}
