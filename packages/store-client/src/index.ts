export * from "./createDatabase";
export * from "./createDatabaseClient";
export * from "./utils";

// TODO: we need a version of the config that doesn't include enums but just primitive types
// so we can map it to the js primitive types
