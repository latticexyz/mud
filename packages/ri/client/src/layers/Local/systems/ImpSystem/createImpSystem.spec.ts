describe("Imp System", () => {
  it("The Imp system handles spawning imps once a second until the max population is reached.", () => {
    // The current imp system is more of a demo than an actual system and will change very soon,
    // so no need to implement a test for it.
    expect(true).toBe(true);
  });
});
