export { mount, type MountOptions } from "../vanilla/mount";
export { mountButton } from "../vanilla/mountButton";

// TODO: export readonly variant?
// TODO: upgrade zustand so we can get the readonly variant
export { store, type Store } from "../vanilla/store";
