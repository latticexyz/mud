// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

library Allowances {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "erc20-store", name: "Allowances", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x746265726332302d73746f7265000000416c6c6f77616e636573000000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0020010020000000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (address, address, address)
  Schema constant _keySchema = Schema.wrap(0x003c030061616100000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint256)
  Schema constant _valueSchema = Schema.wrap(0x002001001f000000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](3);
    keyNames[0] = "tokenAddress";
    keyNames[1] = "account";
    keyNames[2] = "spender";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](1);
    fieldNames[0] = "approval";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get approval.
   */
  function getApproval(
    address tokenAddress,
    address account,
    address spender
  ) internal view returns (uint256 approval) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get approval.
   */
  function _getApproval(
    address tokenAddress,
    address account,
    address spender
  ) internal view returns (uint256 approval) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get approval.
   */
  function get(address tokenAddress, address account, address spender) internal view returns (uint256 approval) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get approval.
   */
  function _get(address tokenAddress, address account, address spender) internal view returns (uint256 approval) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set approval.
   */
  function setApproval(address tokenAddress, address account, address spender, uint256 approval) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((approval)), _fieldLayout);
  }

  /**
   * @notice Set approval.
   */
  function _setApproval(address tokenAddress, address account, address spender, uint256 approval) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((approval)), _fieldLayout);
  }

  /**
   * @notice Set approval.
   */
  function set(address tokenAddress, address account, address spender, uint256 approval) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((approval)), _fieldLayout);
  }

  /**
   * @notice Set approval.
   */
  function _set(address tokenAddress, address account, address spender, uint256 approval) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((approval)), _fieldLayout);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(address tokenAddress, address account, address spender) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(address tokenAddress, address account, address spender) internal {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(uint256 approval) internal pure returns (bytes memory) {
    return abi.encodePacked(approval);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(uint256 approval) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(approval);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(
    address tokenAddress,
    address account,
    address spender
  ) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](3);
    _keyTuple[0] = bytes32(uint256(uint160(tokenAddress)));
    _keyTuple[1] = bytes32(uint256(uint160(account)));
    _keyTuple[2] = bytes32(uint256(uint160(spender)));

    return _keyTuple;
  }
}
