// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { SomeAbstractProgram } from "../../SomeAbstractProgram.sol";
import { revertWithBytes } from "@latticexyz/world/src/revertWithBytes.sol";
import { IWorldCall } from "@latticexyz/world/src/IWorldKernel.sol";
import { SystemCall } from "@latticexyz/world/src/SystemCall.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { Systems } from "@latticexyz/world/src/codegen/tables/Systems.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";

type SomeAbstractProgramType is bytes32;

// equivalent to WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: "", name: "SomeAbstractProg" }))
SomeAbstractProgramType constant someAbstractProgram = SomeAbstractProgramType.wrap(
  0x73790000000000000000000000000000536f6d65416273747261637450726f67
);

struct CallWrapper {
  ResourceId systemId;
  address from;
}

struct RootCallWrapper {
  ResourceId systemId;
  address from;
}

/**
 * @title SomeAbstractProgramLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library SomeAbstractProgramLib {
  error SomeAbstractProgramLib_CallingFromRootSystem();

  function abstractExecute(SomeAbstractProgramType self) internal {
    return CallWrapper(self.toResourceId(), address(0)).abstractExecute();
  }

  function abstractExecute(CallWrapper memory self) internal {
    // if the contract calling this function is a root system, it should use `callAsRoot`
    if (address(_world()) == address(this)) revert SomeAbstractProgramLib_CallingFromRootSystem();

    bytes memory systemCall = abi.encodeCall(_abstractExecute.abstractExecute, ());
    self.from == address(0)
      ? _world().call(self.systemId, systemCall)
      : _world().callFrom(self.from, self.systemId, systemCall);
  }

  function abstractExecute(RootCallWrapper memory self) internal {
    bytes memory systemCall = abi.encodeCall(_abstractExecute.abstractExecute, ());
    SystemCall.callWithHooksOrRevert(self.from, self.systemId, systemCall, msg.value);
  }

  function callFrom(SomeAbstractProgramType self, address from) internal pure returns (CallWrapper memory) {
    return CallWrapper(self.toResourceId(), from);
  }

  function callAsRoot(SomeAbstractProgramType self) internal view returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), WorldContextConsumerLib._msgSender());
  }

  function callAsRootFrom(SomeAbstractProgramType self, address from) internal pure returns (RootCallWrapper memory) {
    return RootCallWrapper(self.toResourceId(), from);
  }

  function toResourceId(SomeAbstractProgramType self) internal pure returns (ResourceId) {
    return ResourceId.wrap(SomeAbstractProgramType.unwrap(self));
  }

  function fromResourceId(ResourceId resourceId) internal pure returns (SomeAbstractProgramType) {
    return SomeAbstractProgramType.wrap(resourceId.unwrap());
  }

  function getAddress(SomeAbstractProgramType self) internal view returns (address) {
    return Systems.getSystem(self.toResourceId());
  }

  function _world() private view returns (IWorldCall) {
    return IWorldCall(StoreSwitch.getStoreAddress());
  }
}

/**
 * System Function Interfaces
 *
 * We generate an interface for each system function, which is then used for encoding system calls.
 * This is necessary to handle function overloading correctly (which abi.encodeCall cannot).
 *
 * Each interface is uniquely named based on the function name and parameters to prevent collisions.
 */

interface _abstractExecute {
  function abstractExecute() external;
}

using SomeAbstractProgramLib for SomeAbstractProgramType global;
using SomeAbstractProgramLib for CallWrapper global;
using SomeAbstractProgramLib for RootCallWrapper global;
