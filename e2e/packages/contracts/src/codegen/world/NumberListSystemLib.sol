// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

import { IWorldKernel } from "@latticexyz/world/src/IWorldKernel.sol";
import { WorldContextConsumerLib } from "@latticexyz/world/src/WorldContext.sol";
import { ResourceId, WorldResourceIdLib } from "@latticexyz/world/src/WorldResourceId.sol";
import { RESOURCE_SYSTEM } from "@latticexyz/world/src/worldResourceTypes.sol";

import { INumberListSystem } from "./INumberListSystem.sol";

library Utils {
  bytes14 constant namespace = "";
  bytes16 constant name = "NumberListSystem";

  function systemId(bytes14 _namespace) internal pure returns (ResourceId) {
    return WorldResourceIdLib.encode({ typeId: RESOURCE_SYSTEM, namespace: _namespace, name: name });
  }

  function systemId() internal pure returns (ResourceId) {
    return systemId(namespace);
  }
}

/**
 * @title NumberListSystemLib
 * @author MUD (https://mud.dev) by Lattice (https://lattice.xyz)
 * @dev This library is automatically generated from the corresponding system contract. Do not edit manually.
 */
library NumberListSystemLib {
  function set(uint32[] memory list) internal {
    IWorldKernel(WorldContextConsumerLib._world()).call(
      Utils.systemId(),
      abi.encodeCall(INumberListSystem.set, (list))
    );
  }

  function push(uint32 num) internal {
    IWorldKernel(WorldContextConsumerLib._world()).call(
      Utils.systemId(),
      abi.encodeCall(INumberListSystem.push, (num))
    );
  }

  function pushRange(uint32 start, uint32 end) internal {
    IWorldKernel(WorldContextConsumerLib._world()).call(
      Utils.systemId(),
      abi.encodeCall(INumberListSystem.pushRange, (start, end))
    );
  }

  function pop() internal {
    IWorldKernel(WorldContextConsumerLib._world()).call(Utils.systemId(), abi.encodeCall(INumberListSystem.pop, ()));
  }
}
