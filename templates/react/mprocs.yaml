procs:
  contracts:
    cwd: packages/contracts
    shell: pnpm run dev
  client:
    cwd: packages/client
    shell: pnpm run dev
  account-abstraction:
    cwd: packages/account-abstraction
    shell: docker-compose up --build
