out/
cache/
node_modules/
bindings/
artifacts/
abi/
types/
broadcast/

# Ignore all deploy artifacts
deploys/**/*.json
# Except the latest ones (so we can load these into client)
!deploys/**/latest.json
