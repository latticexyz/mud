# Batch calls

- Batch calls via `batchCall` and `batchCallFrom` allow multiple system calls to be performed in a single atomic transaction.
- They return a list of return values of the system calls.
