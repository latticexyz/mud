{
  "name": "docs",
  "version": "1.0.0",
  "description": "mud.dev docs",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "start": "next start",
    "build": "pnpm build:prepare && next build",
    "build:prepare": "rm -rf pages/packages && mkdir pages/packages && for PKG in solecs network recs react services; do mkdir ./pages/packages/$PKG && cp -r ../packages/$PKG/API ./pages/packages/$PKG/API; done"
  },
  "private": "true",
  "license": "",
  "dependencies": {
    "next": "^13.3.0",
    "nextra": "^2.4.0",
    "nextra-theme-docs": "^2.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
