import { Callout } from "nextra/components";

<Callout type="info">

This feature is not yet supported by `selectFrom`.
You can get the same effect by running queries and then processing the results in Typescript.

</Callout>
