{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "dist/**",
        "abi/**",
        "types/**",
        "build/**",
        "bin/**",
        "protoc/**",
        "protobuf/**",
        "src/codegen/**",
        "src/mud-definitions/**"
      ]
    },
    "create-mud#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["package.json", "src/**", "../../templates/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false,
      "outputs": [
        "dist/**",
        "abi/**",
        "types/**",
        "build/**",
        "bin/**",
        "protoc/**",
        "protobuf/**",
        "src/codegen/**",
        "src/mud-definitions/**"
      ]
    }
  }
}
