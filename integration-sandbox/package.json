{
  "name": "mud-integration-sandbox",
  "private": true,
  "scripts": {
    "build": "pnpm recursive run build",
    "clean": "pnpm recursive run clean",
    "initialize": "pnpm recursive run initialize",
    "test": "pnpm recursive run test"
  },
  "engines": {
    "node": "18.x",
    "pnpm": "8.x"
  }
}
